name: Release

on:
  push:
    tags:
      - 'v*'    # Triggers on v2.0.0, v2.1.0-beta, etc.

permissions:
  contents: write    # Required to create releases
  id-token: write    # Required for npm OIDC trusted publishing

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          registry-url: 'https://registry.npmjs.org'

      - name: Install build tools
        run: npm install -g terser csso-cli

      - name: Build
        run: |
          chmod +x scripts/build.sh
          bash scripts/build.sh "${{ github.ref_name }}"

      - name: Package release zip
        run: |
          cd build
          zip -r ../frametrail-${{ github.ref_name }}.zip .
          cd ..
          echo "Package size: $(du -sh frametrail-${{ github.ref_name }}.zip | cut -f1)"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: frametrail-${{ github.ref_name }}.zip
          generate_release_notes: true
          body: |
            Download `frametrail-${{ github.ref_name }}.zip` and extract it. FrameTrail works in three modes:

            **Server mode** — Run `php -S localhost:8080` (PHP 7.4+) and open `http://localhost:8080`. Follow the setup wizard. For public deployments use Apache or nginx with PHP-FPM.

            **Local folder mode** (no server) — Open `index.html` in Chrome or Edge and select a `_data` folder when prompted.

            **In-memory mode** — Open `index.html` in any browser. Edit and export via Save As.

            For full documentation and source code, see the [repository](https://github.com/OpenHypervideo/FrameTrail).

            **CDN (jsDelivr):**
            ```
            https://cdn.jsdelivr.net/npm/@frametrail/frametrail@${{ github.ref_name }}/frametrail.min.js
            https://cdn.jsdelivr.net/npm/@frametrail/frametrail@${{ github.ref_name }}/frametrail.min.css
            ```

      - name: Publish to npm
        run: npm publish build/ --access public --provenance
